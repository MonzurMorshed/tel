export default {
    footer(state) {
        return state.footer
    },

    header(state) {
        return state.header
    },

    error(state) {
        return state.error
    },
    sidebar: (state) => state.sidebar
}

