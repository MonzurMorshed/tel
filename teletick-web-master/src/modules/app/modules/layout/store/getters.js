export default {
    layout(state) {
        return state.active
    },
    footer(state) {
        return state.footer
    },

    header(state) {
        return state.header
    },

    error(state) {
        return state.error
    },
}
